# PLC Data Collector Environment Configuration
# Copy this file to .env and configure your settings

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Choose ONE database type by setting the appropriate variables

# SUPABASE CONFIGURATION (Cloud Database)
# Uncomment and configure these for Supabase:
# SUPABASE_URL=https://your-project-id.supabase.co
# SUPABASE_KEY=your-anon-key-here

# SQLITE CONFIGURATION (Local Database)
# Uncomment and configure this for SQLite:
# SQLITE_DB_PATH=./data/plc_data.db

# ===========================================
# APPLICATION SETTINGS
# ===========================================

# Default scan rate for PLC tags (seconds)
DEFAULT_SCAN_RATE=1.0

# Number of days to keep historical data
HISTORICAL_DATA_RETENTION_DAYS=30

# Docker environment flag (set automatically in Docker)
DOCKER_ENV=false

# ===========================================
# NOTES
# ===========================================
# 1. For first-time setup, run the application and use the database wizard
# 2. The wizard will create a database_config.json file with your settings
# 3. You can override the wizard settings by setting environment variables
# 4. SQLite is recommended for local development and testing
# 5. Supabase is recommended for production deployments
